package Main;

import UkrainianCode.Авторизація;

public class Authorization extends Авторизація {
    public boolean authorize(DataBase dataBase) {
        return super.авторизуватися(dataBase);
    }
}
